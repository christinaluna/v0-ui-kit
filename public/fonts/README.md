This folder is reserved for self-hosted font files (e.g. `.woff2`).

The UI kit currently uses `next/font/google` (Inter, Space Grotesk, JetBrains Mono),
but you can drop custom fonts here and wire them up in `app/layout.tsx`.


